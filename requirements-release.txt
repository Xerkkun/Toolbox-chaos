# Exact Qt for Python versions used by official binary release builds.
# Keep Essentials, Addons, and Shiboken on one coherent version.
PySide6-Essentials==6.11.1
PySide6-Addons==6.11.1
shiboken6==6.11.1
# Freeze the validated scientific engine used by Toolbox release evidence.
hidden-attractors-fo==1.2.0
# Freeze the complete cross-platform scientific and plotting runtime used by
# the audited Windows artifact.  The release workflow uses Python 3.14.6.
numpy==2.5.2
scipy==1.18.0
numba==0.67.0
llvmlite==0.49.0
matplotlib==3.11.1
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.5.0
Pillow==12.3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0
pyqtgraph==0.14.0
PyYAML==6.0.3
packaging==26.3
colorama==0.4.6; sys_platform == "win32"
# Freeze the executable builder and its hook set for official native bundles.
PyInstaller==6.22.0
pyinstaller-hooks-contrib==2026.6
altgraph==0.17.5
pefile==2024.8.26; sys_platform == "win32"
pywin32-ctypes==0.2.3; sys_platform == "win32"
# Freeze only the top-level Python distribution tools used by the release job.
# Their transitive dependencies remain resolver-managed.
build==1.4.0
wheel==0.46.3
twine==6.2.0
cyclonedx-bom==7.3.1
setuptools==84.0.0
